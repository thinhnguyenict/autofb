# Video upload test notes

Do not store access tokens in this repository. Use a token obtained through the
approved Meta OAuth flow and pass it through environment variables or the secure
application configuration when testing a video upload.
