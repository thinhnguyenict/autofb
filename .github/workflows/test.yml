name: test
on:
  push:
  pull_request:
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run repository checks
        run: make check
      - name: Install dashboard test dependencies
        run: |
          python -m pip install -r requirements-dev.txt
          python -m playwright install --with-deps chromium
      - name: Run FastAPI integration smoke
        run: python tools/fastapi_smoke.py
      - name: Compile developer tools
        run: python -m py_compile tools/fastapi_smoke.py tools/capture_dashboard.py tools/bootstrap_dev_tools.py
      - name: Capture dashboard artifact
        run: make screenshot
      - name: Verify dashboard artifact
        run: |
          python - <<'PY'
          from pathlib import Path
          assert Path("output/dashboard.png").read_bytes().startswith(b"\x89PNG")
          assert Path("output/dashboard.png").stat().st_size > 10_000
          print("dashboard artifact verified")
          PY
